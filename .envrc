use flake

# Source .env and .env.local if they exist
if [ -f .env ]; then
  dotenv .env
fi

if [ -f .env.local ]; then
  dotenv .env.local
fi

# Watch .env and .env.local for changes
watch_file .env
watch_file .env.local
